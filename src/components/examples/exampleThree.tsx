
const Input = () => <input />

const ExampleThree = () => {


    return <Input />
}

export default ExampleThree;

// 1. Paste below code into App.tsx and check if types are considered the same
// 2. Then move Input into ExampleThree scope and check again

//   console.log(ExampleThree().type === ExampleThree().type)
